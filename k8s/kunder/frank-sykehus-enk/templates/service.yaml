apiVersion: v1
kind: Service
metadata:
  name: {{ include "frank-sykehus-enk.fullname" . }}
  labels:
    {{- include "frank-sykehus-enk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frank-sykehus-enk.selectorLabels" . | nindent 4 }}
