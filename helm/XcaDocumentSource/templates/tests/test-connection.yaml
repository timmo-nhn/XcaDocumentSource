apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xcadocumentsource.fullname" . }}-test-connection"
  labels:
    {{- include "xcadocumentsource.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xcadocumentsource.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
