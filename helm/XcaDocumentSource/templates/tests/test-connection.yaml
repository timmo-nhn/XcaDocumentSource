apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "auditlog-processor.fullname" . }}-test-connection"
  labels:
    {{- include "auditlog-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "auditlog-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
